int f(int x) {
    if ( x <= 1) {
        return 1;
    } else {
        return x * f(x - 1);
    }
}
int main() {
    int x;
    x = 5;
    writeln f(0);
    writeln f(1);
    writeln f(2);
    writeln f(x);
    writeln f(10);
}